import { StyleSheet } from "react-native";

export const BackgroundVideoStyles = StyleSheet.create({
  video: {
    width: "100%",
    height: "100%",
    // flex: 1,
    // position: "absolute",
    // top: 0,
    // left: 0,
  },
});
